# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
*.swp
*.swo

# Build artifacts (future)
node_modules/
dist/
.next/
.cache/

# Environment
.env
.env.local

# Legacy single-file builds (reference only, don't deploy)
silver-key-realty-v3.html

# Generated at build time (contains Supabase keys from Vercel env vars)
assets/js/admin-config.js

# Admin seed SQL (local only, never deploy)
admin/seed-admin.sql
.vercel
.env*.local
